/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router is stable in Next.js 13+, no experimental flag needed
  // SWC minification is the default in Next.js 13+
}

export default nextConfig;
